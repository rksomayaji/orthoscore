<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rksomayaji.work.orthoscores2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".SettingsActivity" />
        <receiver android:exported="false" android:name=".helper.DownloadBroadcastReceiverHelper">
            <intent-filter>
                <action android:name="com.rksomayaji.work.orthoscores2.DOWNLOAD_UPDATE"/>
                <action android:name="com.rksomayaji.work.orthoscores2.IGNORE_UPDATE"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>